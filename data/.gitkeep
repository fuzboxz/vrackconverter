# Module registry data

This directory contains the fetched and deduplicated module registry.

- `modules.json` - Deduplicated module registry (used by RackConverter)
- `modules_raw.json` - Raw module data before deduplication

These files are updated via GitHub Actions workflow_dispatch.
